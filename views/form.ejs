<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= title %></title>
    <link rel="stylesheet" href="/css/style.css">
</head>
<body>
    <div class="container">
        <div style="display: flex; justify-content: space-between; align-items: center;">
            <h1><%= title %></h1>
            <div style="display: flex; gap: 10px; align-items: center;">
                <a href="/" class="button button-link">All Tools</a>
                <a href="/tool-groups" class="button button-link">Tool Groups</a>
                <form action="/tools/update-external" method="POST" style="margin: 0;">
                    <button type="submit" class="button button-primary" style="background-color: <%= totalEnabledCount > 0 ? '#007bff' : '#6c757d' %>;">
                        Update (<%= totalEnabledCount %>)
                    </button>
                </form>
            </div>
        </div>
        <form action="<%= action %>" method="POST">
            <div>
                <label for="name">Agent Name (e.g., Random Number Specialist):</label>
                <input type="text" id="name" name="name" value="<%= tool.name || '' %>" required>
            </div>
            <div>
                <label for="description">Agent Instructions (Description):</label>
                <textarea id="description" name="description" rows="6" required placeholder="Enter the agent instructions here..."><%= tool.description || '' %></textarea>
            </div>
            <div>
                <label for="code">Python Tool Function Code (Code):</label>
                <textarea id="code" name="code" rows="10" placeholder="Enter the Python @function_tool code here..."><%= tool.code || '' %></textarea>
            </div>
            <div>
                <button type="submit" class="button button-primary">Save Tool</button>
                <a href="<%= tool.id ? '/tool/' + tool.id : '/' %>" class="button button-link">Cancel</a>
            </div>
        </form>
    </div>
</body>
</html> 